<template name="api">
{{#better_markdown}}

<h1 id="api">The Meteor API</h1>

당산의 자바스크립트 코드는 두 가지 환경에서 작동합니다: *client* (브라우저)와 
*server* (서버측 [Node.js](http://nodejs.org/) 컨테이너). 이 API 레퍼런스에선
각각의 기능에 대해 우리는 코드가 작동하는 곳이 client에서만인지 server에서만인지 
*어디서든*인지 구분하여 알려줄 것입니다.
